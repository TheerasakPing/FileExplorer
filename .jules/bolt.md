## 2026-02-17 - Missing Test Scripts
**Learning:** The package.json lacks `lint` and `test` scripts. Validation currently relies on `pnpm build` and manual verification.
**Action:** Be extra careful with changes and rely on build success.
